{
  "$comment": [
    "The performance budget, in GZIPPED BYTES of JavaScript per route (#60).",
    "Enforced by `npm run perf`, which CI runs. See scripts/perf-budget.mjs for what is measured.",
    "",
    "WHY THESE NUMBERS. Each is the measured cost on 2026-08-03 plus about 5% headroom \u2014",
    "enough that a minifier or Vite update does not turn CI red, tight enough that a new",
    "dependency or a second island on a route does not slip in unnoticed. A new island is",
    "roughly 1-3 KB gzipped; a new dependency is usually ten times that. The budget is set to",
    "catch the second without nagging about the first.",
    "",
    "THE FLOOR IS REACT. `client.js` is 57 KB gzipped on its own and every route with an",
    "island pays it, so the interesting number is not the total but the distance above ~60 KB.",
    "A concept page is the heaviest at 72 KB because it mounts the most islands: depth toggle,",
    "viz, mini-map, trail ribbon, trail count, search, and review actions.",
    "",
    "`/` IS BUDGETED AT ZERO, AND THAT IS THE POINT. The landing page ships no JavaScript at",
    "all. It is the page a stranger meets first and the clearest evidence for static-first, so",
    "any island there should have to argue for itself in a pull request rather than arrive.",
    "",
    "HTML IS RECORDED, NOT ENFORCED. It grows with the graph \u2014 /concepts lists every node \u2014",
    "and failing a build for publishing content would be the budget fighting the product.",
    "",
    "TWO FIELDS, TWO JOBS (#286). `html_gzip` is an opt-in LIMIT, null everywhere, so nothing",
    "is enforced; setting it to a number opts that route in, and `--update` refuses to turn a",
    "note into a limit on its own. `html_gzip_measured` is the RECORD, written on every",
    "`--update` and compared by nobody, so a jump shows up in a diff. This file used to say",
    "the numbers were here so a jump would be visible while every one of them was null \u2014",
    "measured each run, printed to the console, and stored nowhere.",
    "",
    "/concepts IS THE ONE THAT MOVES: 37,446 B gzipped at 482 concepts, against 8,363 B for the",
    "heaviest concept page and 2,144 B for the landing page. It lists every node, so it grows",
    "with the graph. The 80 B per concept this line used to assert is NOT a measurement \u2014 it",
    "predates any second data point. `measured_on` and `html_gzip_measured` now move together on",
    "every `--update`, so the next refresh gives a real rate between two dated points.",
    "",
    "RAISING A BUDGET IS A DECISION, AND `--update` NO LONGER MAKES IT (#327). `--update`",
    "refreshes `html_gzip_measured` and leaves every `js_gzip` exactly where the last person to",
    "think about it put it. `npm run perf -- --set-budgets` is the one that moves them, and the",
    "commit message has to say what the extra weight bought.",
    "",
    "WHY THE SPLIT. `--update` used to do both, so refreshing a stale measurement also flattened",
    "every budget to exactly what it measured \u2014 deleting the ~5% headroom on all seven routes at",
    "once, silently, and the next 2 KB then turned CI red for a reason nobody chose. Note the",
    "direction: `--set-budgets` LOWERS a budget that still has slack. The danger is not a budget",
    "creeping up, it is the slack quietly disappearing."
  ],
  "measured_on": "2026-08-19",
  "routes": {
    "/": {
      "js_gzip": 0,
      "html_gzip": null,
      "note": "No islands. Zero is the budget; adding one is a decision.",
      "html_gzip_measured": 2144
    },
    "/concepts": {
      "js_gzip": 67000,
      "html_gzip": null,
      "note": "Search overlay and trail count only. HTML grows with the graph.",
      "html_gzip_measured": 37446
    },
    "/search": {
      "js_gzip": 67000,
      "html_gzip": null,
      "note": "The panel itself. The index is fetched on open, not bundled.",
      "html_gzip_measured": 3602
    },
    "/request": {
      "js_gzip": 69000,
      "html_gzip": null,
      "note": "One form.",
      "html_gzip_measured": 3677
    },
    "/404": {
      "js_gzip": 69500,
      "html_gzip": null,
      "note": "Nearest concepts plus the request form.",
      "html_gzip_measured": 3920
    },
    "/c/*": {
      "js_gzip": 76000,
      "html_gzip": null,
      "note": "The heaviest route, and the worst concept page is the one measured. Depth toggle, viz primitive, mini-map, trail ribbon, trail count, search, review actions.",
      "html_gzip_measured": 8363
    },
    "/t/*": {
      "js_gzip": 70500,
      "html_gzip": null,
      "note": "The whole page is one island (ADR-0008).",
      "html_gzip_measured": 3481
    }
  }
}
