{
  "id": "direct-preference-optimization",
  "title": "DPO",
  "domain": [
    "Alignment",
    "Preference"
  ],
  "bodies": {
    "intuition": "RLHF trains a reward model and then optimises against it with reinforcement learning — two models, two stages, and a great deal that can go wrong. DPO shows the reward model was never needed: the same optimum can be reached by a supervised loss on the preference pairs themselves. One stage, no sampling, no value network.",
    "engineer": "Train directly on preference pairs with a classification-style loss, keeping a frozen reference model for the ratio. Far simpler and cheaper than PPO and widely adopted for that reason. It is not free: it optimises the preferences it was given rather than exploring, so it cannot discover a response better than the ones it was shown.",
    "math": "The RLHF optimum is π*(y|x) ∝ π_ref(y|x)·exp(r(x,y)/β), which inverts to r(x,y) = β·log(π*(y|x)/π_ref(y|x)) + β·log Z(x). That last term depends on the prompt alone, so it cancels between two responses to the same prompt — which is what makes the substitution possible rather than merely tidy. Substituting into the Bradley–Terry loss removes r entirely, leaving −log σ(β·log(π_θ(y_w)/π_ref(y_w)) − β·log(π_θ(y_l)/π_ref(y_l))) — the reward model was implicit in the policy all along."
  },
  "emphasis": {
    "intuition": "the reward model was never needed",
    "engineer": "it cannot discover a response better than the ones it was shown"
  },
  "viz": {
    "primitive": "loss-curve",
    "params": {
      "steps": 1000,
      "params": 1
    },
    "param_controls": [
      {
        "name": "params",
        "min": 1,
        "max": 100,
        "step": 1
      }
    ],
    "caption": "Preference loss over a training run. Drag model size to watch where the run bottoms out."
  },
  "edges": {
    "requires": [
      {
        "id": "reward-model",
        "reviewed": false
      }
    ],
    "adjacent": [
      {
        "id": "rlhf",
        "reviewed": false
      },
      {
        "id": "sycophancy",
        "reviewed": false
      }
    ]
  },
  "citations": [
    {
      "ref": "arXiv:2305.18290",
      "title": "Direct Preference Optimization: Your Language Model is Secretly a Reward Model",
      "url": "https://arxiv.org/abs/2305.18290"
    }
  ],
  "updated_at": "2026-08-18",
  "review": {
    "reviewed_by": "opendroid",
    "reviewed_at": "2026-08-04"
  }
}
