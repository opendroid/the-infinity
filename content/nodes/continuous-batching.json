{
  "id": "continuous-batching",
  "title": "Continuous Batching",
  "domain": [
    "Inference",
    "Serving"
  ],
  "bodies": {
    "intuition": "Batching requests together uses the hardware well, but sequences finish at different times, and a batch that waits for its slowest member leaves most of the accelerator idle. Continuous batching lets a finished sequence leave and a waiting one take its place at the next step, so the batch is refilled constantly rather than assembled once.",
    "engineer": "Schedule at the granularity of an iteration rather than a request. A completed sequence frees its slot immediately and a queued one joins mid-flight, which raises throughput several times over static batching at the same latency. It needs a KV cache that can be allocated and released per sequence, which is what makes paged attention its natural partner.",
    "math": "With static batching of size B and lengths n_i, the batch occupies max_i n_i steps while doing Σ_i n_i tokens of useful work, so utilisation is Σn_i/(B·max n_i) — poor whenever lengths vary. Iteration-level scheduling removes the max from the denominator, and utilisation stops depending on the spread of output lengths."
  },
  "emphasis": {
    "intuition": "refilled constantly rather than assembled once",
    "engineer": "which is what makes paged attention its natural partner"
  },
  "viz": {
    "primitive": "budget-split",
    "params": {
      "idle_slots": 8,
      "share_rest": 8
    },
    "param_controls": [
      {
        "name": "idle_slots",
        "min": 0,
        "max": 64,
        "step": 1
      }
    ],
    "caption": "Batch slots idling for the longest sequence against those doing useful work. Drag the idle count to watch a wider spread in output lengths waste the accelerator."
  },
  "edges": {
    "requires": [
      {
        "id": "autoregressive-decoding",
        "reviewed": false
      }
    ],
    "adjacent": [
      {
        "id": "batch-size",
        "reviewed": false
      },
      {
        "id": "paged-attention",
        "reviewed": false
      }
    ]
  },
  "citations": [
    {
      "ref": "arXiv:2309.06180",
      "title": "Efficient Memory Management for Large Language Model Serving with PagedAttention",
      "url": "https://arxiv.org/abs/2309.06180"
    }
  ],
  "updated_at": "2026-08-04",
  "review": {
    "reviewed_by": "opendroid",
    "reviewed_at": "2026-08-04"
  }
}
