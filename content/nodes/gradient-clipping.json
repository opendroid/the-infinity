{
  "id": "gradient-clipping",
  "title": "Gradient Clipping",
  "domain": [
    "Optimization",
    "Training"
  ],
  "bodies": {
    "intuition": "Occasionally a batch produces an enormous gradient — a strange example, a numerical accident — and one full-size step in that direction can undo hours of training. Clipping caps the size of the update while keeping its direction, so a bad batch costs a small step rather than the run.",
    "engineer": "Rescale the whole gradient when its global norm exceeds a threshold, typically 1.0. Clipping per-parameter instead changes the direction, which is why the global norm form is standard. A run whose loss spikes and does not recover is usually a run without it.",
    "math": "g ← g·min(1, c/‖g‖) for threshold c and global norm ‖g‖ over all parameters. Direction is preserved exactly; only magnitude changes, and only when the threshold is exceeded."
  },
  "emphasis": {
    "intuition": "costs a small step rather than the run",
    "engineer": "which is why the global norm form is standard"
  },
  "viz": {
    "primitive": "update-spectrum",
    "params": {
      "threshold": 1.0,
      "bars": 8
    },
    "param_controls": [
      {
        "name": "threshold",
        "min": 0.1,
        "max": 5,
        "step": 0.1
      }
    ],
    "caption": "Per-batch gradient norms before clipping and after. Drag the threshold down to watch the outliers flattened while the rest pass through untouched."
  },
  "edges": {
    "requires": [
      {
        "id": "backpropagation",
        "reviewed": false
      }
    ],
    "adjacent": []
  },
  "citations": [
    {
      "ref": "arXiv:1211.5063",
      "title": "On the difficulty of training Recurrent Neural Networks",
      "url": "https://arxiv.org/abs/1211.5063"
    }
  ],
  "updated_at": "2026-08-02",
  "review": {
    "reviewed_by": "opendroid",
    "reviewed_at": "2026-08-04"
  }
}
